# Phaser Farm Migration — Checklist

Phases are ordered so each is independently shippable behind the `PHASER_FARM` flag and testable in the dev harness. File references point at the React source being ported (the parity reference) — read it before porting it.

Conventions:

- ✅ a phase is "done" when it passes the parity screenshot diff for its surface **and** its interactions dispatch identical machine events.
- The game layer (`src/features/game/events/`, `gameMachine.ts`, `landscapingMachine.ts`) is never modified to make a phase work.

---

## Phase 0 — Foundation ✅ (2026-08-26)

The architecture end-to-end, before any breadth. Everything after this is repetition of these patterns.

- [x] Module scaffold `src/features/farmEngine/` per [ARCHITECTURE.md](./ARCHITECTURE.md)
- [x] `PHASER_FARM` feature flag in `src/lib/flags.ts` (`testnetLocalStorageFeatureFlag("phaserFarm")` — auto-on for amoy/local dev, localStorage opt-in on mainnet)
- [x] Flag-gated swap at the existing `/farm` route in `src/features/game/expansion/Game.tsx`
- [x] Phaser.Game config: no physics, **no `arcade.debug`**. Lives in `FarmPhaser.tsx` (small enough not to warrant a separate `core/engine.ts` yet). ~~pixelArt + Scale.RESIZE~~ superseded by the project-ii rendering model (DPR backing store + `Scale.NONE`, LINEAR + per-texture NEAREST) — see the Phase 1 entry and ARCHITECTURE.md
- [x] `core/coordinates.ts` — grid↔world, y-inversion, `oX/oY` offsets, gameboard dims/bounds; unit-tested against `MapPlacement.tsx` maths
- [x] `core/camera.ts` — drag-pan, wheel + two-pointer pinch zoom 0.5–1, initial centre on origin, sessionStorage one-shot restore, gameboard bounds tracking expansion count. Anchor projection derived from scroll+zoom (NOT `worldView` — it's stale in the tick that moves the camera). _Browser-verified: pan; wheel/pinch still need hand-testing on device_
- [x] `bridge/GameBridge.ts` + `bridge/subscriptions.ts` — `dispatch`, selector `subscribe` with equality fns (unit-tested), UI-prefs store pushed from `GameProvider` context, typed landscaping access, `openModal` wired from `ModalProvider` (its `GlobalModal` type is now exported)
- [x] `bridge/anchors.ts` + `bridge/useWorldAnchor.ts` + `overlay/FarmOverlay.tsx` — anchor registry, camera-driven reprojection, `WorldAnchored` positioner. _Browser-verified: a React label pinned to grid (0,0) tracks pan/zoom_
- [x] `core/clock.ts` — batched 1s-pass stage clock with `useNodeTimer` semantics via `boostWindows.ts` imports (unit-tested, both timing models)
- [x] `core/assets.ts` — URL-as-key image/spritesheet queue + awaitable loader (first real use: Phase 1)
- [x] `entities/EntityRenderer.ts` + `entities/registry.ts` — base contract + empty registry
- [x] Dev tooling: `dev/DebugGrid.ts` (grid/origin/genesis outline + demo anchor, localStorage `phaserFarm.debug`) + `dev/DevPanel.tsx`. Offline farm = run dev server with `VITE_API_URL=` blank (ART*MODE). \_Island/season/expansion switcher deferred to Phase 1 where the art matrix needs it*
- [x] `dev/parity.ts` — canvas snapshot + dependency-free pixel diff (full harness grows with Phase 1)
- [x] Engine teardown verified in-browser: navigate `/farm` → `/home` → `/farm`, exactly one canvas after remount, no engine console errors. HUD renders over the canvas (no z-index on the engine wrapper — `HudContainer` portals at `z-10` and must win)

## Phase 1 — Land & environment (static world parity) ✅ core (2026-08-26)

Reference: the background stack at the top of `src/features/game/expansion/Land.tsx`.

New this phase: the **in-world modal bridge** (`bridge.farmModal` + `overlay/FarmModals.tsx` — Phaser renders the sprite and the click, React renders the modal) and the **NPC pipeline** (`entities/npc/NPCSprite.ts` — composited idle bumpkins from the animation service's 96×64 spritesheets, the same pipeline the world's BumpkinContainer uses).

> **The boundary rule (decided during this phase):** rendering anything gameplay-related is Phaser — swimmers, boats, rafts, NPCs, the expand icon, the pontoon, all of it. React renders UI only: HUD, modals, tooltips, popovers, anchored labels/progress bars. An earlier "world plane" pattern that hosted whole in-world React components over the canvas was removed for violating this; the anchored-overlay scale trick survives strictly for UI (see `overlay/UpcomingExpansionUI.tsx`).

- [x] `OceanLayer` — tiled ocean at 64 src px/tile; winter `frozenOcean`, volcano+ `darkOcean` variants
- [x] `LandBaseLayer` — `getLandImage` (imported from `LandBase.tsx`), origin-centred at scale 1; reacts to expansion / biome / season slices
- [x] `BackgroundIslandsLayer` — 8 islands, 1536×1088 design-board scaling incl. the DOM's CSS-px rounding
- [x] `CloudsLayer` — 4 stretched static frame bands + 32 dynamic clouds; `animate-float` bob (6 CSS px, 3s sine) as tweens gated on `showAnimations` via the UI-prefs bridge
- [x] Water decor — `layers/WaterDecorLayer.ts`: goblin, snorkler (click → modal), seasonal swimmers + cossies, frozen swimmer, mushroom island, full-moon fins (drift tweens), shark fin (45s spritesheet cycle + click → modal). _Parity gap: goblin/snorkler/swimmer source assets are animated GIF/WebP — Phaser shows their first frame until spritesheet art exists_
- [x] Boats — `layers/BoatsLayer.ts`: Pete's teaser raft [TravelTeaser], restock boat [RestockBoat], Discord boat incl. sail tween + Wobble [DiscordBoat], Grubnuk's upgrade raft [IslandUpgrader, position table exported as `getUpgradeRaftPosition`], La Tomatina raft. All clicks → React modals via `farmModal` / global DISCORD modal. Browser-verified: Pete + Grubnuk NPCs render from animation-service sheets; Pete click opens the tabs modal. _Gaps: Discord hull GIF static; Pete's speech-bubble hint text not yet rendered (chat icon only) — needs the engine's speech-bubble treatment; NPC pixel-alignment eyeballed, needs a side-by-side check_
- [x] Upcoming expansion — `entities/UpcomingExpansionRenderer.ts`: expand icon (+pulsate), pontoon, land-complete + reveal marker, reveal flow (events + confetti + BETTY/FIREPIT modals) all Phaser; requirement labels + construction ProgressBar are anchored React (`overlay/UpcomingExpansionUI.tsx`); requirements/speed-up modals in `FarmModals`. _Gap: icon pulsate ignores VIP coin discount (labels use the real hook); camera doesn't re-centre after island upgrade yet_
- [i] NPC animation-service note: sheets only allow CORS from localhost:3000 — local smoke tests must run the dev server on the default port
- [x] Rendering model reworked to project-ii's (see ARCHITECTURE.md "Rendering model"): DPR-supersampled backing store + `zoom: 1/DPR`, global LINEAR with per-texture NEAREST opt-in (`core/assets.ts`), ocean re-tiled via RenderTexture stamps (TileSprite blurs NEAREST in WebGL), modal-open input gating. Browser-verified at deviceScaleFactor 2: uniform native-res texels, pan + click mapping intact
- [x] `DirtLayer` — pure derivation in `layers/dirtTiles.ts` (imports `getGameGrid` + `getDirtImage`), unit-tested (isolated plot, run caps, crop↔path joining, biome art); sprite projection browser-untested — the offline farm has no crops (verify when Phase 2 plants one, or via Phase 8 path placement)
- [x] Parity diff across matrix ✅ — superseded by the Phase 10 matrix runner (10 island/season/expansion combos + all 9 biomes verified)

## Phase 2 — Crops (the interactive vertical slice) ✅ core (2026-08-26)

Reference: `src/features/island/plots/Plot.tsx` + `components/`. This phase sets the template every entity follows — review it hardest.

New this phase: bridge channels for entity UI — `hover` (pointer-over entity → React popovers), `fx` (transient world events → floating +N), `cropReward` (chest/captcha handoff with a result callback) — plus per-plot anchors (`cropAnchorId`), the shared `core/clickable.ts` hover effect, `core/sounds.ts` (plant/harvest Howls), and a crop fixture in `STATIC_OFFLINE_FARM` (2 empty plots, ready Sunflower, growing Kale + seeds).

- [x] `entities/crops/CropRenderer.ts` registered for `state.crops`; per-id reconciliation (plant/harvest/removal; move re-syncs come free when Phase 8 dispatches them). Clicks land on a tile-sized zone (DOM parity: the tall crop art is not clickable), with the hover effect applied to the art
- [x] Soil & growth stages — imported `getGrowthStage` + `CROP_LIFECYCLE[biome]` art at the DOM's −12px offset; non-fertile plot (dry soil + water-well modal). _Deferred: Tornado/Tsunami/GreatFreeze weather-plot art, tutorial dig/click pulsate icons_
- [x] Both timing models via `getHarvestMetrics` (now exported from `FertilePlot.tsx`); boost windows unioned from `getCropPlotBoostWindows` + `getCropFertiliserWindows`
- [x] Clock-driven stage transitions — one `FarmClock` registration per planted plot, stage fractions [0.25, 0.5, 1]. _Nuance: clock progress ignores landscaping-banked `boostedTime` (art may flip a beat early on lifted windowed crops; overlay % is exact)_
- [x] Corner status icons (boost ⚡, weather, bee swarm, fertiliser) — in-scene Phaser images in `CropRenderer` (moved from React per the game-layer ruling)
- [x] Click-to-plant with `selectedItem`, click-to-harvest — same events + payloads (`seed.planted` with uuid cropId, `crop.harvested`, `plot.fertilised`), double-click buffer, analytics milestones, BLACKSMITH/seasonal-seed modals. Browser-verified: plant + harvest round trip. _Deferred: quick-select popup when no seed is selected_
- [x] `TimerPopover` on hover via the hover channel + plot anchor — browser-verified ("Sunflower 58secs" with icon). The only crop UI left in React
- [x] Progress bar under growing crops when `showTimers` — in-scene `ProgressBarSprite`, browser-verified ("58s"/"1d")
- [x] Harvest FX: floating `+N` with `getYieldColour` — in-scene `playYieldFloat` (browser-verified); proc firework spritesheet ≥10 yield in Phaser (logic ported, needs a high-yield farm to eyeball)
- [x] Chest reward / captcha flow — `cropReward` channel → `ChestReward` anchored at the plot, harvest completes via callback (wired; needs a reward roll to eyeball)
- [x] Event-payload parity test vs `Plot.tsx` dispatches ✅ — superseded by `scripts/dispatch-parity.js` (7 events, ALL PASS)

## Phase 3 — Trees & mineral nodes ✅ core (2026-08-27)

Reference: `src/features/game/expansion/components/resources/*`. All follow recovered → depleting (strike anim + `+N`) → depleted (stump/hole + `TimerPopover` countdown) → recovered.

New this phase, following two further boundary rulings: **progress bars/inline timers render in-scene** (`components/ProgressBarSprite.ts` — the DOM Bar art + pixel-font time label; crops + pontoon + lava pits use it) and **game-layer labels render in-scene** (`components/LabelSprite.ts` chips — expansion requirement labels are now Phaser; `components/YieldFloat.ts` — the +N floats are Phaser; crop corner status icons are Phaser). React overlays are now hover-popovers + modals ONLY (`CropsUI` = crop popover; `ResourcesUI` = node popovers, no-tool warnings, chest-reward host). Shared node plumbing in `entities/resources/ResourceNodeRenderer.ts` (per-id reconcile, DOM stale-read touch counts, outside-click reset, health bars, hover channel).

- [x] Shared base: `ResourceNodeRenderer` + config-driven `MineralRenderer` (one class, five family configs)
- [x] Trees (`entities/resources/TreeRenderer.ts`) — shake sheet, chop sheet, stump + recovery timer, biome/season `alternateArt` variants, Insta-Chop, Foreman-Beaver tool rules, chest-reward captcha. Browser-verified: 3-click chop → stump → "Tree 1hr 59mins" hover popover
- [x] Stone / Fused / Reinforced (`STONE_CONFIG`) — tier art + tier strike sheets, Quarry/Tap-Prospector rules
- [x] Iron / Refined / Tempered (`IRON_CONFIG`)
- [x] Gold / Pure / Prime (`GOLD_CONFIG`)
- [x] Crimstone stages 1–6 (`CRIMSTONE_CONFIG`, imported `getCrimstoneStage`), stage-6 triple-drop sheet, Crimstone Spikes Hair wearable rule
- [x] Sunstone stages 1–10 (`SUNSTONE_CONFIG`, imported `getSunstoneStage`), deliberate `now <= readyAt` depletion parity
- [x] Ascension Crystal — single-use, mining ghost (drop sheet + `+3` float at the removed node's box)
- [x] Oil Reserve — full/half/empty by remaining WORK, spurting-well bonus overlay, single-click drill
- [x] Lava Pit — idle/running(bar)/ready(alert) + modal (`LavaPitModalContent` reused). _Gap: running art is an animated webp — static in Phaser_
- [x] Boulder — static + teaser modal
- [x] Advanced-tier art/offsets per the READONLY table (all 9 rock names)
- Fixture: `STATIC_OFFLINE_FARM` now has iron/gold/crimstone/sunstone/oil nodes + all pickaxes/drill
- [x] Parity round 2026-08-29: recover lightning-flash ✅ (`scheduleBoostFlash` on external choppedAt/minedAt changes), Pete speech bubble ✅ (`components/SpeechBubbleSprite.ts`, speechBorder nine-slice 2/2/4/5), no-tool warnings via i18n ✅ (`t("craft") tool`). _Chip border art was already done in the bugfix round._

## Phase 4 — Compound & derived resources ✅ core (2026-08-27)

Modal hosts extended: `overlay/FarmModals.tsx` gained flower/beehive/salt/fisherman/water-trap modals (+ `overlay/farmModalContents.tsx` for the ported inline contents); `ResourcesUI` gained a `CompoundPopover` for the new hover kinds. Browser-verified on the offline farm: apple harvest → `+1` HUD delta + replenishing art + in-scene 11h bar + "Apple Replenishing" popover; mushroom pick → `+1`; blueberry 5h bar; ready/empty flower beds; beehive quantity bar; salt farm platform with in-scene UPGRADE chip + goblin; fisherman idle on the wharf.

- [x] Fruit Patch (`FruitPatchRenderer`) — seedling/growing/replenishing/replenished/dead states, harvest + dead-tree chop (wood reward float), fertilise, boost windows. Browser-verified harvest round trip
- [x] Flower Bed (`FlowerBedRenderer`) — `FLOWER_VARIANTS` growth art, cross-breed modal (`FlowerBedModal` reused), insta-grow + congratulations modals
- [x] Beehive (`BeehiveRenderer`) — `beehiveMachine` reduced to a 1s tick (honey %, ready drop indicator), Bee.tsx flight as a tween chain (exact geometry/durations), harvest + swarm modal
- [x] Mushrooms (`MushroomRenderer`) — 5-frame idle sheet with random ≤15s replay gap, ALWAYS_ON_TOP band, one-click pick (browser-verified)
- [x] Salt farm (`SaltRenderer`) — derived positions, upgrade flow modal (`UpgradeSaltFarmModalPanel` reused), in-scene UPGRADE label chip
- [x] Fisherman + dock (`FishermanRenderer`) — per-island wharf art + offsets, bubbles/fish decor, 56-frame sheet as idle→casting→waiting→reeling→caught state machine, cast/caught modals
- [x] Water trap spots (`WaterTrapRenderer`) — wharf-derived coords, crab-spot art, 2-frame pot loops, soak bar, sparkle+alert when ready, placement/caught modals
- [x] Yield-float icon loading hardened: `playYieldFloat` now lazy-loads a missing icon texture (was Phaser's black missing-texture square), and Tree/Mineral/AscensionCrystal/FruitPatch queue their float icons up front
- Fixture: `STATIC_OFFLINE_FARM` now has fruit patches (ready Apple, growing Blueberry), flower beds (ready Red Pansy + empty), a half-full beehive, a Wild Mushroom, and a crab-trap spot
- [x] Fruit quick-select ✅ (2026-08-29): empty-patch click without a plantable seed opens the DOM QuickSelect disc row (`bridge.quickSelect` -> ResourcesUI, anchored at the patch, DOM geometry bottom-20/left-10). GOTCHA: engine clicks fire on pointerdown while QuickSelect's outside-click closer is a document mousedown listener — the open must be deferred (setTimeout 0) or the same event instantly closes it. Fixture: empty patch "3" at (-5,6) + patch-fruit seeds in inventory.
- [x] Parity round 2026-08-29: harvest shake ✅ (0.82s x-wobble), fisherman puzzle gate ✅ (`fishingChallenge` modal hosting FishermanPuzzle, onDone callback via farmModal data), frenzy/full-moon/marvel icons ✅. _Still deferred: compact DOM-parity styling of the beehive-level/flower-congrats/fisherman-caught modals (functional versions exist); crab-spot art eyeball_

## Phase 5 — Buildings ✅ core (2026-08-27)

Reference: `src/features/island/buildings/components/building/Building.tsx`.

Architecture: one `entities/buildings/BuildingRenderer.ts` reconciling all of `game.buildings`, with the per-building art/offsets flattened to data in `buildingArt.ts` (`BUILDING_BASE_ART` + `COOKING_LAYOUT`, Phaser-free/jest-testable). Building clicks route through new `FarmModalName`s to `overlay/BuildingModals.tsx`, which reuses the DOM's exported modals (`FirePitModal`…`SmoothieShackModal`, `ShopItems`, `WorkbenchModal`, `ComposterModal`, `CraftingBoxModalContent`, `FishMarketModal`, `AgingShedModal`, `CropMachineModal` + local `cropStateMachine`, `UpgradeBuildingModal`, `Constructing`, `WeatherAffectedModal`). Navigation buildings go through the new `bridge.navigateTo` (react-router wired in FarmPhaser). Browser-verified: Town Center/Market/Workbench base art; Fire Pit cooking state (0.8 alpha, doing-NPC, item icon, ready `!` alert) → click opens the Fire Pit modal; constructing Kitchen at 50% + in-scene bar → click opens the Constructing panel ("01:58:42" + Speed up); running Compost Bin (composting art + in-scene timer bar); Market click → Betty conversation + shop.

- [x] Building renderer registry: texture per building × construction state, biome/season/level variants (`alternateArt.ts`, `BARN_IMAGES`, `COMPOSTER_IMAGES`, aging-shed thresholds, water-well upgrade-adjusted level)
- [x] Constructing state: 50% alpha + bottom-centre in-scene progress bar + Constructing modal with gem/coin speed-up (`building.spedUp`/`upgrade.spedUp`)
- [x] Click → correct React modal per building; level gate → lock panel; destroyed (tornado/tsunami badge) → `WeatherAffectedModal`; collect-on-click parity (`recipes.collected` / `processedResource.collected`)
- [x] Cooking/processing indicators: NPC idle/doing swap, per-building cooking item icon (DOM anchor formulas), ReadyRecipes/ReadyProcessed floating rows, ready `!` alert with shake, Bakery smoke, Smoothie Shack desk overlay, composter/crafting-box bars, greenhouse smoke + ready-plant row, aging-shed ready alert
- [x] Interior entry points: Hen House/Barn/Greenhouse/Pet House routes + Town Center/House/Manor/Mansion via `getHomeRoute` (wired; routes themselves not browser-tested offline)
- Fixture: `STATIC_OFFLINE_FARM` buildings = INITIAL trio + cooking Fire Pit + constructing Kitchen + running Compost Bin
- [x] Parity round 2026-08-29: crop-machine stage sheets + ready overlay + icon row ✅; hen-house/barn alert rows ✅; house extras ✅ (daily chest → DAILY_REWARD, HomeBumpkins row, LetterBox modal via refactored `LetterBoxModals`, collect heart via `bridge.onGameEvent`); market sale labels + first-sale helpers ✅; water-well auto-open on `building.upgraded` ✅. Tent bumpkin = N/A (dead code — DOM's walletBumpkins is hardcoded `[]`). _Fish-market idle Neville ✅ 2026-08-30 (NPCSprite at the DOM's (-4, 11))._ **GIF statics resolved 2026-08-30** — see the animated-art entry in Phase 10.

## Phase 6 — Collectibles (breadth via registry) ✅ core (2026-08-27)

Reference: `src/features/island/collectibles/Collectible.tsx` + 369 components in `collectibles/components/` + ~330 inline entries in `CollectibleCollection.tsx`.

Architecture: one `entities/collectibles/CollectibleRenderer.ts` over `game.collectibles`, driven by the GENERATED `staticCollectibles.ts` table (535 entries: width/bottom/left/right/centeredIn/shadow per item; `art` omitted where the placed art is byte-identical to `ITEM_DETAILS[name].image` — 504 items — with 31 explicit imports for trophy/case variants). The table was produced by extraction scripts now preserved in `docs/phaser-farm-migration/scripts/` (re-run `extract-collectibles.js` → `extract-inline.js` → `crosscheck.js` → `gen-static-table.js` when the DOM components change). Template pieces (`TemplateCollectible` + `DECORATION_TEMPLATES`) and un-tabled specials fall back to ITEM_DETAILS art at natural width, bottom-centred; `PlaceableFlower` widths use the DOM's name-pattern rule. Browser-verified: Basic Bear, 3-Fence autotiled run (end caps), stacked Blue Tiles (connected art), Rug in the low depth band, Red Carnation, constructing Scarecrow (50% alpha + bar).

- [x] Audit: marker-based classification of all components — ~535 static (incl. 24 animated-gif statics), 4 true spritesheet, ~25 state-reading/interactive
- [x] Static renderer — metadata-driven, zero per-item code; `flipped` → `setFlipX`; shadow underlays; in-progress = 50% alpha + centred bar + speed-up modal (`collectibleConstructing` → `Building` panel from Collectible.tsx, `collectible.spedUp`)
- [x] Sheet support — declarative config; Squirrel Monkey loops (26×32, 12fps, 9 steps)
- [x] Fence autotiling — reuses the DOM's exported `getFenceImage`/`getStoneFenceImage`/golden variants over `getGameGrid` (crops + collectibles)
- [x] Tiles via `getTileImage` (connected-below check); Dirt Path skipped (painted by `DirtLayer`)
- [x] Depth parity: tiles lowest band, rugs/furniture (`NON_COLLIDING_OBJECTS`) next, everything else painter's `y`
- [x] Expiring boosts ✅ (2026-08-29): Time Warp Totem, Super Totem, all 7 hourglasses — `entities/collectibles/expiringCollectibles.ts` config + `renderExpiringNode` in CollectibleRenderer (staged full/half/done art, buff->error countdown bar, pulsing fast-forward while active, grayscale via preFX ColorMatrix once expired, dig icon -> `collectible.burned`, "!" -> new `renewCollectible` farm modal hosting the DOM RenewCollectible). Browser-verified all three states + both click flows. Fixture: three boosts at (3..5,7). Still deferred from this family: the active-state SFT detail popover (SFT popovers are their own deferred group).
- [x] Stateful collectibles ✅ (2026-08-29): reveals (Genie Lamp confirm→REVEAL→genieRevealed, Maneki Neko canShake→REVEAL, Festive Tree season gate; `overlay/RevealModals.tsx`), Monuments (staged PROJECT_IMAGES art + quantity cheer bar + completion modal + cheers popover), Beds (staged art + unlock-farmhand modal), Salt Sculpture (leveled art + upgrade modal), Obsidian Shrine + all pet shrines (extended EXPIRING config: activeBar/renewFlow/alertTop + `renewPetShrine`/`obsidianShrine` modals), Bush season variants, Wicker Man/Tomato Bombard click-burst sheets + Rock Golem state sheets (`CLICK_SHEETS` + `renderRockGolem`), SFT detail popovers on EVERY collectible + buds + active boosts (`bridge.sftPopover` + `overlay/SftPopoverUI.tsx`), ScarecrowAOEOverlay in landscaping (`drawAoeOverlays`, shared getAOEExtent). _Weather-protection `used` alerts ✅ 2026-08-30 (grayscale + "!" + renewWeather modal)._ **GIF statics resolved 2026-08-30** — see the animated-art entry in Phase 10.

## Phase 7 — Characters & dynamic entities ✅ core (2026-08-27)

Renderers in `entities/characters/`: `PlayerRenderer` (placed bumpkin + farm hands via `NPCSprite`'s animation-service idle sheets; click zones are 16×32 tile boxes, NOT the 96px sheet frame — sprite-level hit areas swallowed neighbours' clicks), `PetRenderer` (NFT pets from `state.pets.nfts` — CDN 44×44 sheets idle 0-8 @8fps, sleeping webp when napping/neglected/type-fed, egg pre-reveal — AND placed common pets, whose placement is a collectible entry but whose logic lives here; `CollectibleRenderer` skips pet names; `PETS_STYLES` raw offsets exported from petShared), `BudRenderer` (CDN webp per token id, shadow + 32-wide art at (-8,-16), Retreat nudge), `AirdropRenderer` (chest bulge tween + floating alert). Modal hosts in `overlay/CharacterModals.tsx` reuse `BumpkinModal` (feed tab), `BumpkinEquip` (farm hand, dispatches `farmHand.equipped`), `PetModal`, `AirdropModal` (owns `airdrop.claimed`). Browser-verified: both bumpkins idle-animating, Barkley placed, airdrop alert, bumpkin click → BumpkinModal (Feed/Equip/Skills/Info), farm hand click → equip modal, Barkley click → PetModal (energy/level/feed/fetch).

- [x] Placed Bumpkin — NPCSprite composition, flipped, click → BumpkinModal
- [x] Farm hands — same pipeline, click → equip modal
- [x] Buds — `location === "farm"` filter, CDN art (animated webp = first frame in Phaser)
- [x] Pet NFTs (2×2, idle sheet anim + asleep/egg states, per-type alert icon positions) + common pets (PETS_STYLES offsets, happy/asleep art, `pet.pet`/`pet.neglected` clicks)
- [x] Airdrops — bulge + float animations, click → ClaimReward modal via bridge (coordinate-less airdrops stay with Game.tsx's AirdropPopup)
- [x] Depth: all use painter's `ENTITY_BASE + y` band
- [x] Parity round 2026-08-29: auras ✅ (back/front 20×19 8f 14fps in NPCSprite), tutorial click helpers ✅ (plots dig/click, market, PlayerNPC), bud click popover ✅ (via sftPopover). N/A-by-DOM: pet XP floats (DOM doesn't render them), NFT pet walking (walking pref unconsumed). _PlayerModal/Discovery on visit ✅ and the 0.78px bumpkin delta ✅ (2026-08-30; measured against the DOM at 2 CSS px)._

## Phase 8 — Landscaping mode ✅ core (2026-08-27)

References: `placeable/landscapingMachine.ts` (unchanged), `placeable/Placeable.tsx`, `collectibles/MovableComponent.tsx`, `landscaping/LandscapingGrid.tsx`.

Architecture: `landscaping/LandscapingController.ts` drives the UNCHANGED landscapingMachine — it renders the mode chrome + ghost + selection tint in-scene, converts pointer/keyboard input to grid coordinates via camera math (replacing every DOM measurement), and sends the machine the DOM's exact events. The React `LandscapingHud` (chest, quick panel, PlaceableController confirm/cancel, removal-mode buttons, 60s autosave) mounts unmodified from FarmPhaser (`isLandscaping ? <LandscapingHud/> : <Hud/>`). Existing-item moves commit straight to gameService with `getMoveAction` payloads (now exported), collision always via imported `detectCollision` over `removePlaceable`-stripped state. `farmCamera.panSuspended` stops pan-fighting during drags; `makeClickable` is inert while `scene.landscapingActive` (the DOM's READONLY swap). Browser-verified end-to-end: hammer → dim + tile-aligned grid + landscaping HUD; chest → Letter A Tile → ghost seeded at camera centre → arrow-nudged with per-cell collision (red on crops/gold rock/constructing scarecrow footprints — all correct) → Enter placed it at (3,3) → exit; select Basic Bear → tint + disc row → arrow-move committed `collectible.moved` → shovel → confirm → bear removed.

- [x] `LandscapingController` — enter/leave, 42px-aligned grid (red in removal mode), black dim, chrome teardown
- [x] Ghost placement — camera-centre seed, drag + arrow/WASD nudge, `p` pixel-perfect (1/16-tile fractional coordinates), per-move `detectCollision` → `UPDATE`, green/red tint, DRAG/DROP; confirm/cancel/Enter/Escape stay with the React PlaceableController
- [x] Multi-place flow — machine + PlaceableController handle `nextOrigin` re-seeding (origin-aware ghost)
- [x] Move — hit-test over ALL placements (buildings/collectibles/resources/buds/pets/farm hands/bumpkin, frontmost-by-y), `MOVE` select, drag or arrow nudge → `getMoveAction` dispatch with the DOM's exact payload shapes (resources omit name+location, NFTs use `nft`, oX/oY preserved), `BLUR` on outside click
- [x] Removal mode: `TOGGLE_REMOVAL_MODE` grid tint + one-click `REMOVE` via `getRemoveAction`; `REMOVE_ALL` via the HUD's existing two-step button
- [x] Flip/remove disc row — anchored React overlay (`overlay/LandscapingUI.tsx`) at the selection anchor, two-step remove, flip for collectibles/FarmHand/Bumpkin
- [x] Collision parity: imported `detectCollision` + `removePlaceable` — zero re-implementation
- [x] `LandscapingHud` unmodified over the canvas (chest/shop/layouts/cancel column all live)
- [x] Parity round 2026-08-29: drag-preview art on selections ✅ (ghostArt at the target cell), pixel-perfect oX/oY for existing items ✅ ("p" + arrows, ±8 src px, committed through the move payload), overlap disambiguation menu ✅ (`bridge.overlapMenu` + picker in LandscapingUI), Kuebiko/Hungry-Caterpillar warnings ✅ (`removeWarning` modal, both removal paths). Quick-panel drag-to-place was already fixed by the gridPointer override (stale entry). READONLY resource ghost art ✅ 2026-08-30 (`landscaping/readonlyResourceArt.ts`, extracted by `scripts/extract-readonly-resources.js`; ghost + drag preview honour the DOM's left/right/top/bottom offsets). _Still deferred: mobile long-press (verify in the mobile hand-test); saved-layouts browser verification_
- [x] Hide during landscaping ✅ (2026-08-29): WaterDecor/Boats/UpcomingExpansion/Fisherman/Salt/WaterTrap renderers add `landscaping` to their slices and clear on entry, rebuilding on exit [Land.tsx:1302-1334] — verified 175→153→176 objects across enter/exit (exit must go through the LandscapingHud X; parent BACK is not the exit event)

## Phase 9 — Visiting mode ✅ core (2026-08-27, code-complete; live-API browser pass pending)

Key inversion (gameMachine loadLandToVisit): while visiting, `context.state` = the VISITED farm (renders as-is through the same renderers), `context.visitorState` = your own farm, `isVisiting = context.visitorId !== undefined`. The engine adds `scene.visitingActive` (subscription in FarmScene) and `makeClickable`'s `visitClickable` opt-in — everything is click-dead on a visit except the DOM's `enableOnVisitClick` set.

- [x] `/visit/:id` route flag-forks to `<FarmPhaser key="visit"/>`; FarmPhaser renders `VisitingHud` when visiting (third HUD branch)
- [x] Click gating parity: opt-ins = collectibles (all), pets (common + NFT), clutter, home-set buildings (Town Center/Tent/House/Manor/Mansion/Pet House); everything else inert
- [x] `ClutterRenderer` — trash/dung/weed + pests (10px art, pests below garbage above ALWAYS_ON_TOP), hidden once `hasHelpedPlayerToday`; click → `garbage.collected {id, totalHelpedToday}` (local-only; loot lands in the VISITOR's inventory) → `FarmHelped` modal on `isHelpComplete`
- [x] Help flows: pets → `pet.visitingPets` (help disc drawn while `!visitedAt`); monuments/projects → `project.helped` (names in `REQUIRED_CHEERS`, skip if `helpedAt`); Pet House → `pet.helpAllPetsInHouse` when tasks pending, else navigate `/visit/:id/pet-house`; home buildings navigate `getHomeRoute({isVisiting: true})`
- [x] `farmHelped` FarmModalName → the DOM's `FarmHelped` (its OK button owns the `farm.helped` effect that persists the help server-side)
- [x] Local-only exclusion verified in gameMachine (LOCAL_VISITING_EVENTS filtered from the actions queue at :436-439) — machine untouched
- [x] Parity round 2026-08-29: clutter sparkle ✅ (world sparkle.png sheet 20×19 7f @6fps), monument cheer bars ✅ (render for visiting too), camera storage split ✅ (`:visit` suffix key). _Still deferred: live-API browser verification (needs account); VisitorGuide auto-open verification_

## Phase 10 — Parity QA & the swap (in progress — harness ✅ 2026-08-27)

Tooling landed: the DevPanel now has the island/season/expansions switcher (writes `phaserFarm.dev.*` localStorage keys read by `applyPhaserDevOverrides` in landDataStatic.ts — deferred since Phase 0, unblocks the matrix); `window.__farmGame` is exposed in dev builds for perf probes; `scripts/parity-matrix.js` loops the matrix, screenshots each combination, and samples FPS.

- [~] Full-matrix screenshot pass: first 10-combination run (basic/spring/desert/volcano × seasons × expansions 3–42) captured + eyeballed — desert & volcano/winter render correct biome/season art throughout. React-farm side captured too (`react` arg; `localStorage phaserFarm="off"` force-disables the flag even on dev builds — added to `testnetLocalStorageFeatureFlag`). **Pixel alignment solved (2026-08-29)**: the React boot is modal-free now (the fixture pre-stamps from the dev-QoL round cover the old terms-checkbox blocker), and the react branch re-centres `#genesisBlock` deterministically (the app's boot scrollIntoView races layout) — cross-correlation of Phaser vs React basic-spring-3 lands at **offset dx=0 dy=0** at DPR-2 buffer resolution, residual ~6.8/255 mean diff from animation frames (water/clouds/walkers). `ONLY=basic-spring-3` env re-captures one combo. **Landscaping-on shots ✅** (`scripts/landscaping-shots.js`, both engines via `__gameService.send("LANDSCAPE")`): grid/dim/HUD/discs match; deltas are the known Phase-8 deferred items (DOM swaps buildings to READONLY art — e.g. Fire Pit menu-board variant — and hides the fisherman during edit). **All 9 biomes ✅** (2026-08-29): the 5 override biomes (Swamp/Spooky/Crystal/Galaxy/Marble Age via `phaserFarm.dev.biome`, now also a DevPanel select) render correct terrain + tree variants at ~100fps. Remaining: a freeze-animations toggle if we want near-zero diffs. _Note: rapid successive runs hit CDN 429 rate limits (missing land base in late shots) — space runs out or warm the cache_
- [~] Event-dispatch parity suite: `scripts/dispatch-parity.js` — records `window.__gameService.send` (dev hook in GameProvider), targets entities by GRID coordinates through the live camera (no hardcoded screen positions), normalises volatile fields, asserts expected payloads. Seed suite (crop.harvested / fruit.harvested / mushroom.picked) ALL PASS. **Already caught a real parity bug**: the engine depth-sorted by box bottom while the DOM sorts by placement row (`Land.tsx` `b.props.y - a.props.y`) — the Town Center's box swallowed clicks (and paint) for the crops row below it. Fixed across all renderers (images AND input zones now use `ENTITY_BASE + box.y`). **Extended 2026-08-29 to 7 events ALL PASS** (+ flower.harvested, beehive.harvested, timber.chopped incl. 3-tap shake, stoneRock.mined) — and caught two more real bugs: (1) six renderers (Pet/Player/Clutter/Salt/WaterTrap/Fisherman) never set their click-zone depth, so an unset (depth 0) zone swallowed clicks for every entity placed below row 0 — Barkley's zone was eating stone 1's taps; (2) the stress egg caught CropRenderer's 100ms double-click buffer being renderer-global instead of per-plot. Fixture additions for the suite: beehive "2" (full honey) at (2,-2), `farmActivity["Red Pansy Harvested"]=1` (first-discovery opens the congratulations modal instead of dispatching). Remaining: tool-selection flows (fertilise via seed selection), landscaping-mode dispatches
- [~] Perf: **~120fps at every matrix point incl. 42-expansion volcano** (M-series laptop, DPR 2) — double the 60fps target. **Memory stability ✅ at harness level** (2026-08-29): farm↔interior↔farm ×3 — display list (172), textures (205), and canvas count return exactly to baseline every cycle; heap wobble ±5MB is GC noise. Fixed en route: the `window.__farmGame` dev hook wasn't cleared on unmount, pinning the destroyed game. New `dev/PerfPanel.tsx` (top-centre, dev builds): fps + 5s-low, frame ms, long-task count (jank), display-list objects, live textures, JS heap, renderer/zoom — the leak canaries live on screen now. Remaining: mid-range mobile hand-test; long-session soak
- [x] Stress-test Easter egg (2026-08-29): DevPanel 💥 toggle (`phaserFarm.dev.stress` + forces Land 42) → fixture carpets 99 plots of ready Sunflowers (the level-4 well fertility cap — `getSupportedPlots` returns 99 max; forces well level 4 + a placed Water Well) + 10k Sunflower Seeds for replanting; `dev/StressBumpkins.ts` spawns 50 wandering bumpkins (4 NPC outfits, walking anim frames 9–16 @10fps, tween wander loops with flipX + per-tick row depth). Verified: 113–119fps with the crowd; 45/46 spam-taps harvested (the 46th was a chest-reward plot — correct modal flow). **Caught a real parity bug**: CropRenderer's 100ms double-click buffer was renderer-global, but the DOM's `clickedAt` is per-Plot-component — rapid taps across *different* plots were swallowed. Now a per-plot Map.
- [x] Animated art (GIF → spritesheet) ✅ (2026-08-30): `scripts/gif-to-spritesheet.js` decodes every GIF the farm renders into a vertical-strip PNG via sharp/libvips (which composes frame disposal correctly) and emits the GENERATED `core/animatedArt.ts` manifest keyed by the ORIGINAL art URL — so lookups work wherever art is resolved, in dev and prod. 50 strips (33 local + 17 CDN, downloaded and stored locally at **216KB total**) live in `src/assets/animations/`; moving them to the CDN later only changes the manifest's import lines. `core/animated.ts` wraps it: `queueArt` loads the strip instead of the image, and `resolveArtObject` hands back a looping Sprite (recreating the object when the kind changes, since an Image can't animate). Wired through WaterDecor, Boats, UpcomingExpansion (pontoon), Buildings (cooking NPCs + smoke), Collectibles, and the WaterTrap sparkle; landscaping ghosts/drag-previews use frame 0 (`add.image(key, 0)` — a spritesheet's base texture is the whole strip). GOTCHA that cost a debug cycle: guards like `textures.exists(art.texture)` must switch to `artTexture(url)`, because the original GIF URL is no longer what gets loaded. Verified: **zero `.gif` textures remain**, 28 sprites playing on the harness, 38k pixels changing between frames 700ms apart, ~113fps. New `phaserFarm.dev.gifs` harness key places every animated collectible in a grid; the converter warns if new GIF art lands in ITEM_DETAILS without being converted (Squirrel Monkey / Tomato Bombard are listed as deliberate exclusions — they already animate from purpose-built sheets)
- [ ] Beta rollout: flag → `betaFeatureFlag`, gather feedback, burn down bug list *(needs Adam / live account)*
- [ ] Flip default; React farm behind an escape-hatch flag for one release
- [ ] Delete: `Land.tsx` render tree, `MapPlacement.tsx`, `MovableComponent.tsx`, `Placeable.tsx` DOM path, `DirtRenderer.tsx`, per-resource React renderers (keep the pure logic they imported), `GameBoard`/`ZoomProvider` if nothing else uses them
- [ ] Docs: update ARCHITECTURE.md to reflect as-built reality; write the interiors plan

## Phase 11 — Post-parity upgrades (the payoff — separate efforts, not part of "port")

- [ ] Texture atlas packing for farm assets
- [ ] Day/night & weather shader pass (farm-appropriate; recolour-not-glow per pixel-art style rules)
- [ ] Particle/tween polish: harvest, chop, mining feedback
- [ ] Camera niceties: momentum pan, smart zoom-to-cursor
- [ ] Explore: crop sway, water animation, ambient wildlife

## Phase 12 — Interiors (ALL 6 surfaces ✅ — only visiting variants left)

- [x] Surface architecture ✅ — `core/surface.ts` defines `FarmSurface` (`PlaceableLocation` plus the non-placement rooms `greenhouse`/`barn`/`henHouse`) and `isPlacementSurface`; `InteriorBackdropLayer` holds one `ROOMS` table keyed by surface (every interior's DOM shell centres its room art on the origin, so the layer just needs art + size + an optional nudge), and `FarmPhaser` maps surface → renderer set. Adding a surface is now a room entry + a renderer list + a route fork.
- [x] **Home interior ✅ (2026-08-30)** — `/home` forks to `<FarmPhaser surface="home" />`. The recipe from the plan held: `FarmScene` now takes `{key, location, renderers}`, so one scene class serves both surfaces; `HOME_RENDERERS` swaps the world layers for `layers/InteriorBackdropLayer.ts` (repeating exterior backdrop + room art sized grid+walls, and the clickable wall painting) and reuses CollectibleRenderer / BudRenderer / PetRenderer / PlayerRenderer unchanged. Every placement renderer now picks its slice through `collectiblesAt(game, location)` / an `onThisSurface` filter rather than hardcoding the farm. `LandscapingController` is fully location-parameterised (collision, remove, move payloads, and its placement index skips resources off-farm), so placing inside the house works end-to-end — verified: room + placed rug + painting + bumpkin row + exit, then LANDSCAPE → SELECT → ghost on a valid tile with confirm/cancel. `overlay/InteriorUI.tsx` keeps the unplaced-bumpkin row and exit button as anchored React. GOTCHA: anchor interior chrome to the room ART box (grid + walls), not the inset placement grid, or it overlaps the room.
- [x] **Pet house ✅ (2026-08-30)** — `/pet-house` forks to `surface="petHouse"`; room art per level, `collectiblesAt` gains the `petHouse` case (its `pets` map is the surface's collectibles), placed pets render through the existing PetRenderer.
- [x] **Greenhouse ✅ (2026-08-30)** — `/greenhouse` forks to `surface="greenhouse"`. Not a placement surface: `entities/greenhouse/GreenhousePotRenderer.ts` pins the four pots at the DOM's fixed offsets (verified landing on the exact px), stages plant art off the shared boost-window timing, and handles plant/harvest clicks + progress bar + boost lightning.
- [x] **Animal houses — FULL INTERACTION ✅ (2026-08-30)** — the per-animal xstate machines are gone by design (Adam's call: "feel free to change that"). `entities/animals/animalInteraction.ts` is the DOM's click decision tree (Cow/Sheep/Chicken were three ~700-line copies) as ONE pure class over game state dispatching the same events: feed (+required-qty check, golden-asset auto-feed, feed-buff apply), love (auto-selects the love item), cure (Oracle Syringe / Barn Delight with cost), claim (mutant-modal intercept, 1.4s drop animation with the DOM's 0/500/1400ms sound timing), sleeping/locked → detail modals, plus the 0.5s post-event modal guard. The machine's two visual-only bits became renderer transients: the 2s happy/sad post-feed reaction and the drop animation (also replayed on bulk Feed All claims detected via ready→sleeping underneath us). Renderer widgets: request bubbles (icon+qty, `components/RequestBubbleSprite.ts`), level bar + level number, emotion icons (ready/zZ/happy/sad at DOM offsets), feed-buff badge, over-capacity lock, mutant sparkles (7-frame sheet), 1s info messages, XP floats. Chrome stays React in `overlay/AnimalHouseUI.tsx` (anchored): FeederMachine, FeedAllButton, shop/upgrade discs + their modals, and the bounty-exchange deal flow via a new `bridge.animalDeal` store (sell tab sets deal → renderer dims invalid animals + reports the clicked id → AnimalDeal modal; ExchangeHud replaces the Hud). VERIFIED headless: claim → `produce.claimed`, cure → `animal.fed` Barn Delight, love → `animal.loved` Petting Hand, feed → `animal.fed` Kernel Blend → cow happy +140xp with icon. New modal hosts: animalDetails/animalLocked/mutantAnimal; new sounds: feed/collect×3/produce_drop/cure/level_up. GOTCHA: `bridge.selectItem` reaches `bridge.ui.selectedItem` via a React render, so a click in the same tick reads the stale item.
- [x] **`/interior` + `/level_one` ✅ (2026-08-30)** — turned out NOT to need a coordinate transform: placements use the same centre-origin MapPlacement convention as the farm (the x∈[0,23]/y∈[1,24] convention lives only inside `interiorLayouts` validity checks, which the shared `detectCollision(location)` already handles). So: ROOMS entries (interior = INTERIOR_BACKGROUNDS 380×320, level_one = per-tier HOME_EXPANSION_BACKGROUNDS 384×320, both bottom-left-anchored on the centred 24×24 canvas → computed centre offsets) + `INTERIOR_FLOOR_RENDERERS` (backdrop + the standard placement renderers) + a dark `#181425` void instead of the exterior-island backdrop. `overlay/InteriorFloorUI.tsx` keeps the React chrome: beta/no-upgrade empty states, the in-world UpgradeButton (per-tier positions), and the stairs between floors. Routes fork in Navigation.tsx via `PhaserInteriorRoute` (the flag check needs the machine context, which only exists under GameWrapper). VERIFIED: both floors render at the exact computed geometry (−192,−128), landscaping SELECT on level_one produces a valid ghost with correct collision, PLACE correctly rejected by the reducer for an item not in inventory; the LandscapingHud's own InteriorBumpkins row and floor nav appear because the reused DOM HUD renders them itself. Import-items migration modal fires from the reused HUD too.
- [x] Animal houses — rendering (superseded by the full-interaction entry above) — `/barn` and `/hen-house` fork to `surface="barn"|"henHouse"`. `entities/animals/AnimalHouseRenderer.ts` reproduces the DOM's auto-layout (sort by type then experience, flow across the floor bounds) and derives each animal's look from the `animalMachine` `initial` priority list read straight off game state, so no machine-per-animal is needed. **Interaction NOT ported**: feed / feed-buff / love / cure / claim-produce is a decision tree spread over Cow/Sheep/Chicken (~700 lines each) coordinating a per-animal xstate machine, drop animations and no-food prompts. Rendering-without-interaction is deliberate — a half-ported feed path would dispatch wrong events. Animals currently have no click zone.
- [x] **Interior gap sweep ✅ (2026-08-31)** — the five known small gaps closed: (1) greenhouse oil machine (`entities/greenhouse/GreenhouseOilRenderer.ts` — barrels + oil-count LabelChip + smoke while growing + click → `greenhouseOil` modal hosting the DOM's GreenhouseOilModal); (2) greenhouse fertiliser — pot clicks now try compost first (empty AND growing pots), plant has the seed-validity + oil guards (oil warning chip; quick-select discs via `overlay/GreenhouseUI.tsx` on the shared quickSelect store; the oil guard also applies inside the picker), fertiliser badges (powerup/stopwatch) render on pots — verified headless: planted (oil 12→9), fertilised empty + growing pots; NOTE the engine guards fertilise on inventory count because the raw reducer throws the machine into its error screen; (3) home intro (Pumpkin Pete SpeakingModal, localStorage `home.intro` — verified click-through acknowledges); (4) pet house chrome (`overlay/PetHouseUI.tsx` — pet-guide disc → PetHouseModal, quick pet-all when pets nap (`pet.pet` per napping pet; common-pet placement read from petHouse.pets, behaviour from pets.common), upgrade disc → UpgradeBuildingModal — verified); (5) destroyed-building redirect (barn + greenhouse only, matching the DOM) in InteriorUI. GOTCHA: anchored-overlay components must copy the anchor rect to a local null-safe value before building styles — a stale closure reading a vanished rect crashes the whole overlay tree.
- [x] **Polish pass ✅ (2026-08-31)** — from Adam's play-test list:
  - **Dev Layout presets** (`lib/landLayouts.ts` + DevPanel dropdown): `basic` (9 land, Town Center + 8 crops + 3 trees), `everything` (42 land, every building/collectible/resource), `stress` (42 land + the sunflower carpet). Presets REPOSITION the fixture into non-overlapping bands (one empty tile between neighbours, an empty row between sections) rather than fabricating entries, so authored state survives. Picking a preset clears the manual Land override.
  - **NPC shadows**: NPCSprite drew the 15px asset at native and half a depth-step back. Now 12 source px (the DOM's size) dropped 6px below the boot row, and boat/pontoon NPCs opt into `shadowAboveScenery` so the shadow lands ON the hull instead of behind it — that's why Pumpkin Pete and the upgrade goblin appeared to have none.
  - **Building NPCs follow their building**: `NPCSprite.setPosition/setVisible`; BuildingRenderer repositions its cached NPC each refresh (it was pinned to the box it was created with) and hides the building + NPC while it is the one being dragged, via a new `bridge.landscapingMoving` signal.
  - **1px clickable outline** (`core/hoverOutline.ts`, wired through `makeClickable`'s new `outline` option): four flat-white copies of the art at ±1 world px behind it — hard pixel edges, no glow, exactly 1px at any zoom because world units ARE source pixels. Wired into crops, resource nodes, collectibles and buildings; suppressed in landscaping (selection has its own treatment).
  - **Greenhouse 1:1 pixels**: pot stage art was forced to 28 wide (the "almost" frames are 29 native), the oil icon to 8 (native 7), badges to 5/6 (native 10). All now draw at native scale — a re-audit shows ZERO non-1:1 art in the greenhouse.
  - **Animal request bubble + level bar**: the DOM's `ANIMAL_REQUEST_IMAGES` widths are raw CSS px, not source px, so sizing a bubble from 16 made it ~2.6x too big. Halved to clean 2:1 downscales, and the level bar moved up off the cell bottom to the animal's feet (chickens are ~6px tall in a 32px cell, so the DOM's hang-off-the-bottom left a big gap).
  - **Pixel-perfect placement UI** (was DEFERRED): the controller already had the state, clamping and oX/oY commit behind the "p" key — added `bridge.landscapingControls` so the React disc row drives it, giving the pixel-perfect disc plus four nudge arrows that hide once an axis maxes at ±8. Verified: nudge dispatches `collectible.moved` with `oX: 1`.
  - **Barn / greenhouse "can't click in"**: not an engine bug — the fixture bumpkin had 10,000 XP and those buildings need levels 30 and 46, so the click correctly opened the level-locked modal. Fixture is now level 60 (2,000,000 XP).
  - **Landscaping remove**: verified working in both modes (two-step confirm disc, and one-click under the HUD shovel's removal mode) — the first click showing a confirm tick is the DOM's own behaviour.
- [x] **Native pixel sizing ✅ (2026-08-31)** — Adam's call: draw art at the texture's own size, never resample; the DOM's hardcoded display widths are stale in places. `core/pixelArt.ts` `nativeScale(image, intendedWidth)` sets scale 1 and returns the x-shift that keeps the art's centre where the DOM put it; `applyArt` (every resource node) additionally keeps the BOTTOM edge so sprites still stand on the same ground line. Converted across buildings, collectibles, resources, crops, fruit patches, salt, beehives, flower beds, animals, boats, water decor, expansion pontoon, and the label/bubble/yield components. Audit result: ZERO non-1:1 art on the farm and in every interior (the only entries left are two icons mid-pulse from the alert tween). Notable corrections: barn 72px art was drawn at 64, the greenhouse building 62px at 78, flower beds 52 at 48, the NPC shadow 15 at 12. LEFT ALONE deliberately: the ocean/cloud/background-island layer, which scales responsively against a 1536x1088 design board (parallax backdrop, not grid art), and floor tiles, which must tessellate to exactly 16px.
- [x] **Animated art moved to committed sprite strips ✅ (2026-09-01)** — per Adam, the runtime ImageDecoder approach is GONE (core/remoteAnimated.ts deleted); animated webps/gifs are now pre-converted by the extended `scripts/gif-to-spritesheet.js` into vertical strips in `src/assets/animations/` (408KB total, the folder Adam will migrate into the Images repo): all 43 animated common-pet webps under assets/sfts/pets/ (auto-globbed by ANIM chunk, so new pets are picked up on re-run) + per-token CDN art for the veteran fixture's ids (buds images/<id>.webp, pet sleepings/<id>_animated.webp — bud 1007 skipped, 403 at source; static files skipped automatically). Token entries are keyed in the generated `core/animatedArt.ts` under BOTH mainnet and testnet URLs so lookups match either network. BudRenderer + PetRenderer now go through the existing `core/animated.ts` queueArt/resolveArtObject API like every other renderer; unconverted token ids fall back to the static CDN frame. GOTCHA that bit here: after switching a queue call from queueImage(url) to queueArt(url), any `textures.exists(url)` guard on the ORIGINAL url must go — the strip loads under its own key, so the guard early-returns forever (commons vanished until it was removed). Verified live: bud 933, sleeping Warthog 2921, Meowchi asleep, Barkley/Burro/Twizzle/Mudhorn all looping from `assets/animations/*` textures, frames advancing, 0 errors, gates green.
- [x] **Veteran-farm parity sweep + animated CDN art ✅ (2026-09-01)** — full React-vs-Phaser comparison on Adam's real farm: aligned whole-farm captures (genesisBlock-centred DOM scroll vs cam.centerOn, same 2.625 CSS px/src px), region crop pairs, live display-list + DOM-element probes, and an interaction suite (harvest ✓ ready Saltwort picked + plot cleared, stone mining ✓, SFT + letter-tile popovers ✓, Bakery cake modal ✓, Fish Market ✓, Meowchi neglect dispatch ✓ — its click zone honors the oX/oY nudge, Mansion → /interior ✓). Statics matched everywhere once probe artifacts were ruled out (three "diffs" — oil spurting well, saltwort field, Meowchi pose — were all crop-alignment illusions; the spurting well on every-3rd-drill bonus is IDENTICAL geometry in both). Non-issues: his 3 trees + Barn/Hen House/Pet House are UNPLACED (chest) so those routes 404 in BOTH renderers; bud 1007 403s on the testnet CDN (mainnet fine; DOM shows a broken img). REAL gap found + FIXED: animated CDN art (bud gifs, pet `*_animated.webp`, common-pet webps) played natively by DOM <img> but frozen at frame 0 in Phaser → `core/remoteAnimated.ts` decodes them at runtime via ImageDecoder (WebCodecs) into multi-frame canvas textures + looping anims, cached per URL, static fallback where unsupported; wired into BudRenderer (+ shadow/zone even when art is missing, matching the DOM's clickable broken-img) and PetRenderer (NFT sleeping + common happy/asleep, asleep↔happy swaps handled). Verified live: 7 sprites animating (Warthog sleeping, Meowchi asleep, Barkley/Burro/Twizzle/Mudhorn awake, bud 933 gif), frames advancing, 0 errors. GOTCHA for future probes: zone-probes miss bottom-origin sprites (origin(0,1) puts y below the visual), and side-by-side crops clamp at image edges — re-verify any "diff" with a single-run zoomed screenshot before calling it a bug.
- [x] **Veteran = Adam's REAL export, verbatim ✅ (2026-09-01)** — Adam dropped his full account export (large.json, 264KB, the complete server farm shape) and it now IS `fixtures/veteranFarm.json`; `makeVeteranFarm` just runs it through `makeGame` (the client's own API deserializer) — no synthesis, no approximation. Verified live: username "adam", volcano island with the Basic Biome applied (green land — his real setup), exact 12 expansions, 10 placed buildings, 101 placed collectibles ("ADAM FARM" letter tiles and all), 56 crops, home layout, salt farm, pets; 117fps. Two offline-QoL overrides only: `tcsAcknowledged` stamped fresh, and `season.startedAt` stamped to `getLastTemperateSeasonStartedAt()` because the export's chapter had rolled over and the machine boots into the blocking `seasonChanged` screen otherwise. The old hand-reconstructed JSON + Packer fallback are deleted.
- [x] **Dev panel simplified + veteran exactness ✅ (2026-09-01)** — the panel is now ONLY `Phaser (dev) | Layout` (basic • 9 / everything • 42 / stress • 42 / veteran • volcano 12); the Island/Biome/Season/Land dropdowns are gone from the UI but their localStorage override keys still work — the Playwright parity harness sets them directly. Veteran now uses the account's EXACT land (12 expansions, from the captured inventory) on volcano. `veteranFarm.ts` passes through VERBATIM any placement field present in `veteranFarm.json` (buildings, crops, trees, rocks, home, animals, island, season...): drop the full account export over that file and the farm renders exactly as Adam's, coordinates and all. Until then the fallback places the subset that fits (21 buildings + 68 plots + 16 stones); trees/fruit/iron/gold/crim/sun/oil/decor stay in the chest because the ~436-tile estate only fits 414 buildable tiles with the original hand-packed coordinates. GEOMETRY: 12 expansions [EXPANSION_ORIGINS, 6x6 blocks CENTRED on origins] form a full rectangle x −9..15 / y −9..9 — the spiral's 4th column extends EAST, so the land is not centred on the world origin.
- [x] **"Veteran" preloaded farm ✅ (2026-09-01)** — Adam's captured late-game account as a Layout preset (`fixtures/veteranFarm.json` verbatim + `fixtures/veteranFarm.ts` builder): level-82 bumpkin (11.98M XP, full skill tree incl. Insta-Chop/Instant Growth), 25,916 coins / 1,699 FLOWER / 42,354 Gems, the full ~550-item inventory, wardrobe, chore board and bank; volcano island; his real equipped outfit (Sunburst Potion body, Crimstone Armor, Butterfly Aura) and bumpkin position (8,-8). CAVEATS recorded in the file: the capture hit the 50KB message limit mid-`home.collectibles`, so ALL placements + previous* snapshots were lost — the builder synthesizes a tidy layout from the inventory's placeable counts (21 buildings, 12 showpieces, 68 plots, 23 trees, 15 patches, 16/12/8 rocks, 4+4 crim/sun, 3 oil, hives, beds, lava pit) and bumps land from his true 12 expansions to 30, since 430+ tiles only fit 12 expansions with the original hand-packed coordinates. To restore full fidelity: re-export the account as a FILE (not chat) and drop it over veteranFarm.json with a `placements` pass-through. GOTCHA: the 30-expansion volcano land base (level_30.webp) is ~44 tiles across, origin-centred — probe the art's displaySize for the envelope, don't guess.
- [x] **Hover glow, attempt 3 ✅ (2026-09-01)** — `core/hoverGlow.ts`, a custom WebGL PostFX pipeline wired through `makeClickable`'s `glow` getter (crops, resource nodes, collectibles, buildings). The key move: farm art bakes drop shadows in as SEMI-TRANSPARENT pixels, so anything keyed off `alpha > 0` (Phaser's built-in Glow FX included) halos the shadow blob — this shader thresholds alpha at 0.8 first, so only near-opaque "body" pixels count. Effect = thin white rim hugging the solid silhouette (~1 source px, sampled at `uZoom` framebuffer-px-per-source-px steps so it stays 1 source px at any zoom) + soft warm-yellow falloff outside it (~3 src px) + an 18% brightness lift on the body. Verified on the cherry tree (rim wraps crown/trunk/each falling petal; baked shadow untouched) and the barn (fences rimmed, trampled-dirt base excluded); 114fps while glowing; suppressed in landscaping; pipeline removed on pointerout/click/destroy; no-ops on the Canvas renderer.
- [x] **Hover outline reverted (attempt 2) ✅** — the 1px outline was rejected on sight; `core/hoverOutline.ts` is deleted and `makeClickable` is back to no visual hover treatment (the file records that both a scale-up and an outline were tried and rejected, so it isn't attempted a third time).
- [x] **EXPERIMENT round 4 ✅ (2026-09-01)** — (1) full-body bumpkin click: the composed sprite's visible body runs ~16px above AND below its 16px box (the 64px service frame is centred at y+16), so the 32-tall zone left the legs dead — now 48 tall; (2) the `ui/select_box.png` ring draws around the selected bumpkin's base at native size and follows them as they walk; (3) WALKING JITTER root-caused: every animation was played with the idle's 9-frame range, but each service animation has its OWN count (walking is 8, dig 13) — the out-of-range frame flashed blank every cycle; frames now derive from sheet width / 96; (4) queued markers show the item the job will use — axe on trees, the mineral's own pickaxe (from `config.toolToSelect`), the seed on a plant, the crop on a harvest — icon at native size with the blue dot tucked at its corner (icons pop in after first-time texture load; cached thereafter); (5) PLANT + HARVEST jobs on crop plots: ready → Harvest (`doing` anim → the unchanged plot click), empty → Plant with the selected seed or the first stocked crop seed (`dig` anim → `seed.planted` with a seedOverride param, since bridge.selectItem is stale same-tick). Verified end-to-end: harvest then plant fired `crop.harvested` + `seed.planted`(Sunflower Seed auto-picked). GOTCHA: eslint --fix reformats can silently defeat python replace-anchored edits — verify the string landed (`grep` after), not just that tsc passes.
- [x] **EXPERIMENT round 3 ✅ (2026-08-31)** — the Done panel is gone; a small X (SUNNYSIDE cancel icon) floats above the selected bumpkin's head and deselects on POINTER DOWN. That fixes the "can't always click the button" bug: the anchor tracks a walking bumpkin, so the element moves between mousedown and mouseup and a click pair often never completed — pointerdown fires on press regardless. Verified by clicking the X mid-walk.
- [x] **EXPERIMENT round 2 ✅ (2026-08-31)** — select box now DOCKS UNDER the bumpkin's feet (a `worker-bumpkin` anchor published from `publishAnchor()`, re-asserted as they walk); bare-ground taps play a pixel tap-ripple (three 1px-stroke rectangle outlines stepping inward 12→8→4, hard edges, no tweened strokes so they stay 1px at any zoom); every queued target gets a 4px blue dot (0x0099db — this bumpkin's colour — with a 1px dark rim) that clears when its job runs; and jobs now cover **mushrooms (Dig, `dig` anim → `mushroom.picked`)** and **all five minerals via MineralRenderer (Mine, `mining` anim → `stoneRock/ironRock/goldRock/crimstoneRock/sunstoneRock.mined`, skipping the tap counter on arrival)**. `WorkerJob` gained `anim` + `dotAt`. Verified end-to-end: mushroom→crimstone→sunstone queue fired exactly `mushroom.picked, crimstoneRock.mined, sunstoneRock.mined` in order, dots cleaned up, ripple ring present on ground tap, and a tap that lands on a building's zone correctly defers to the building (the modal opens; no walk). GOTCHA: a Phaser Rectangle with no fill still reports a truthy `fillColor` — probe rings by `name`, not fill.
- [x] **EXPERIMENT: bumpkin job queue ✅ (2026-08-31)** — `worker/BumpkinWorker.ts`. Click your own bumpkin to select them (a readout appears above the dev panel), then click trees to queue chops in click order, or bare ground to queue a walk. The bumpkin walks the route at 60 src px/s using the animation service's `walking` sheet, plays `axe` on arrival, and only THEN fires `tree.chopped` — the underlying game events are untouched. Additive by design: the only engine hook is `worker.intercept(job)` at the top of TreeRenderer's click handler, so with nothing selected every click behaves exactly as before (verified: 3 taps still chop instantly). A worker swing sets `force`, skipping the DOM's 3-tap counter, since a full swing should fell the tree. NPCSprite gained `play()`/`setFlip()`/`origin()`, and the worker re-asserts its position each move because PlayerRenderer rebuilds the bumpkin whenever chopping grants XP. Verified end-to-end: queue 2 trees -> 0 chops on click -> 2 chops after walking.
- [ ] **Art decision for Adam (SUPERSEDED — now fixed above)**: the 1:1 pixel rule can't be finished inside the engine because the DOM scales world art itself — `barn_1.webp` is 72px native drawn at 64 (0.89x), the greenhouse building is 62px drawn at 78 (1.26x), flower beds 52→48, bush 25→28. The engine matches the DOM exactly today; making these 1:1 means changing the art or the DOM's sizes, which would break side-by-side parity with the DOM farm.
- [ ] Visiting variants (`/:id/home`, `/:id/pet-house`, `/visit/:id/interior`, `/visit/:id/level_one`)

## Phase 12+ — Interiors (deferred by decision #3)

Planned properly after land parity; expected shape: one `InteriorScene` sharing all Phase-0 infrastructure, per-interior layers/renderers for `home`, `barn` (+ `animalMachine` animals), `henHouse`, `greenhouse`, `petHouse`, using `HOME_BOUNDS`-family collision imports.
