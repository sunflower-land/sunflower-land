// [TODO]: add more settings types
export type BgMusicPausedControl = {
  isBgMusicPaused: boolean;
};

export type SfxMutedControl = {
  isSfxMuted: boolean;
};

export type MasterVolumeControls = {
  isBgMusicPaused: boolean;
  isSfxMuted: boolean;
};
