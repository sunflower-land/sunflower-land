import { GameObjects } from "phaser";
import { createSelector } from 'reselect'

const crop createSelector

export class Trees extends GameObjects.Container {
  constructor(scene: Phaser.Scene) {
    super(scene)

  }

  create() {
    this.registry.get()
  }

  this.add
}
